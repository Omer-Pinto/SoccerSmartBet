# SoccerSmartBet Environment Configuration
# Copy this file to .env and fill in your actual values

# ============================================================
# Database Configuration (Task 1.2)
# ============================================================
DB_USER=postgres
DB_PASSWORD=your_password_here

# ============================================================
# LangSmith Configuration
# ============================================================
# Enable LangSmith tracing for LangGraph flows
LANGSMITH_TRACING=true

# LangSmith API endpoint (default: https://api.smith.langchain.com)
LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# Your LangSmith API key (get from https://smith.langchain.com/)
LANGSMITH_API_KEY=your_key_here

# Project name in LangSmith dashboard
LANGSMITH_PROJECT=SoccerSmartBet-testing

# ============================================================
# LLM models Configuration
# ============================================================
# OpenAI API Key (for testing with OpenAI)
OPENAI_API_KEY=your_key_here

# ============================================================
# Football Data APIs Configuration (Batch 2)
# ============================================================
# football-data.org API Key
# Register at: https://www.football-data.org/client/register
# Used for: Fixtures, H2H statistics
FOOTBALL_DATA_API_KEY=your_key_here

# The Odds API Key
# Register at: https://the-odds-api.com/
# Used for: Betting lines (500 credits/month free tier)
ODDS_API_KEY=your_key_here

# TheSportsDB API Key
# Free tier key: "3" (public test key)
# Premium: Register at https://www.thesportsdb.com/pricing ($9/month)
# Used for: Venue info, injuries (via lineups), league positions (100 req/min free tier)
THESPORTSDB_API_KEY=3

# Open-Meteo (No API key required)
# Used for: Weather data (10k requests/day, no signup needed)

# ============================================================
# DEPRECATED: APIfootball.com (Trial expiring)
# ============================================================
# apifootball.com API Key - NO LONGER USED
# Trial expires Dec 14, 2025
# Replaced by TheSportsDB for venue, injuries, league position
# APIFOOTBALL_API_KEY=your_key_here

# ============================================================================
# SoccerSmartBet Docker Compose Environment Variables
# ============================================================================
# Purpose: Configuration for PostgreSQL staging and production databases
# Usage:
#   1. Copy this file to .env: cp .env.example .env
#   2. Update passwords with secure values
#   3. Never commit .env to version control
# ============================================================================

# ==========================================================================
# STAGING DATABASE CONFIGURATION
# ==========================================================================
# Staging environment for development and testing
# Default port: 5432 (standard PostgreSQL port)

POSTGRES_STAGING_DB=soccersmartbet_staging
POSTGRES_STAGING_USER=postgres
POSTGRES_STAGING_PASSWORD=change_me_staging_2024!

# ==========================================================================
# PRODUCTION DATABASE CONFIGURATION
# ==========================================================================
# Production environment for live deployment
# Default port: 5433 (non-standard to avoid conflicts)

POSTGRES_PROD_DB=soccersmartbet_prod
POSTGRES_PROD_USER=postgres
POSTGRES_PROD_PASSWORD=change_me_production_2024!

# ==========================================================================
# PORT CONFIGURATION (Optional - Defaults shown)
# ==========================================================================
# Customize if you have port conflicts on your system
# Staging uses standard PostgreSQL port, production uses alternative

STAGING_PORT=5432
PROD_PORT=5433

# ==========================================================================
# SECURITY NOTES
# ==========================================================================
# - Use strong passwords (16+ characters, mixed case, numbers, symbols)
# - Different passwords for staging and production
# - Never commit .env file to git (already in .gitignore)
# - For production deployment, use secrets management (AWS Secrets Manager, etc.)
# - Consider using PostgreSQL role-based access control for application users
# ==========================================================================
